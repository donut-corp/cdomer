fn fib(n: int) -> int {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let i = 0;
    for (let i = 0; i < 10; i += 1) {
        print("fib(", i, ") =", fib(i));
    }
}
